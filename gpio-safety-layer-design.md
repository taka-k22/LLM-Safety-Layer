# GPIO セーフティレイヤ 基本方針（案）

## 目的
LLM が生成した GPIO 制御（ON/OFF、PWM、シリアル風ビット列など）を、実機に適用する前に安全性監査し、危険な命令・不正形式・意図しない動作を遮断する。  
「LLM は提案者」「セーフティレイヤが最終決裁者」という責務分離を前提にする。

## 基本原則
1. デフォルト拒否（Allowlist 方式）  
   許可したピン・許可した操作・許可した範囲以外は実行しない。
2. 形式検証と意味検証を分離  
   まずパース可能か（構文）、次に安全か（セマンティクス）を判定する。
3. 監査ログは必須  
   何を拒否/許可したか、理由、LLM 出力、正規化後命令、実行結果を保存。
4. フェイルセーフ  
   エラー時は GPIO を安全状態（例: 全OFF、モータ停止、PWM=0）へ遷移。
5. リアルタイム/外界入力は LLM に直接渡さない  
   センサ値の使い方やフィードバック制御は別コンポーネントで閉じる（LLM は高レベル指示に限定）。

## 脅威モデル（最低限）
- 誤生成（ピン番号間違い、論理反転、単位ミス、無限ループ、sleep 無し）
- 暴走（高頻度トグル、PWM 100% 常時、繰り返し命令）
- 競合（複数プロセス/スレッドが同一ピンを操作）
- ハード破壊（過電流を誘発する組み合わせ、同時駆動）
- 人身/物理リスク（モータ・ヒータ・レーザー等の危険アクチュエータ）

## 全体アーキテクチャ（責務分割）
- LLM：高レベル意図 → 制御計画（抽象命令の生成）
- セーフティレイヤ：命令の検証・正規化・レート制限・権限管理・実行可否判断
- 実行エンジン（GPIO ドライバ）：セーフティレイヤが許可した命令のみ実行
- 監視（ウォッチドッグ）：ハング/遅延/異常頻度/温度等で緊急停止

## 命令インターフェース方針
LLM の出力は「直接コード」ではなく「宣言的コマンド」に寄せるのが安全。  
例：JSON/YAML などの DSL（GPIO_SET / PWM_SET / PULSE / SEQ など）  
セーフティレイヤは DSL を受け取り、実行可能な最小プリミティブにコンパイルする。

必須の正規化：
- ピン識別子の統一（BCM番号等）
- 値の型（bool/int/float）と単位（ms、Hz、duty 0..1）
- 時間の上限（duration 必須、無限は不可）

## 検証ステージ（多段）
Stage 0: パース/スキーマ検証  
- JSON として妥当か、必須フィールドがあるか、未知フィールドは拒否 or 無視方針を明示

Stage 1: 権限（Allowlist）  
- 許可ピン一覧、ピンごとの許可操作（出力のみ等）
- 危険アクチュエータは “armed” 状態でのみ許可（後述）

Stage 2: 物理制約チェック  
- PWM duty 上限、周波数範囲、連続ON最大時間
- 同時ON禁止の組合せ（Hブリッジ、相互排他ライン等）
- 連続トグル上限（Hz制限）

Stage 3: 実行計画の安全性  
- シーケンスの総時間上限
- ループ命令禁止（反復は回数上限必須）
- sleep / wait の必須化（高頻度暴走防止）

Stage 4: ランタイム監視  
- 実行中のレート制限（token bucket 等）
- 実測のピン状態監査（可能なら）
- タイムアウトで強制停止

## “ARM/DisARM” 概念（危険動作の二段階化）
危険度が高いピン/デバイスは、通常は DISARM（拒否）にしておく。  
ユーザの明示操作または別要件を満たすと一定時間だけ ARM する。
- ARM は短時間 TTL 付き（例: 30秒）
- ARM 状態はログ・UIに可視化
- ARM 中でも上限値は守る（PWM上限等）

## エラー処理と安全状態
- 監査で拒否 → 実行しない（現状態維持 or 安全状態へ）
- 実行中エラー/例外/ウォッチドッグ発火 → 即座に安全状態へ
- 安全状態の定義：機器ごとに明記（全OFFが安全とは限らない場合がある）

## ロギング/監査証跡
最低限ログに残す：
- 入力（LLM出力原文）
- 正規化後コマンド
- 判定（allow/deny）
- deny理由（ルールID）
- 実行結果（成功/失敗、所要時間）
- 実行時の状態（armed 状態、現在のピン占有者）

## 競合対策（リソース管理）
- ピンを「ロック」して単一オーナーのみ操作可能にする
- セーフティレイヤが唯一の GPIO 実行権限を持つ（他プロセスは禁止）
- 可能なら OS 権限/コンテナ分離で直アクセスを潰す

## ルールの表現
ルールはコード直書きより「設定として外だし」するのが運用しやすい。
- allowlist: 許可ピン、許可操作
- constraints: duty/freq/max_on/max_toggle、相互排他グループ
- device profiles: モータ/ヒータ/LED 等のテンプレ

## テスト方針（最初に用意する）
- ユニットテスト：スキーマ不正、境界値、禁止組合せ、無限実行検出
- フェイルセーフテスト：例外発生時に安全状態へ落ちるか
- 侵入テスト：LLMが悪意/誤りで出す典型パターン（ピン逸脱、超高Hz、極端duty、長時間ON）

## 段階的導入（推奨）
Phase 1: DSL + allowlist + 上限制約 + ログ  
Phase 2: ARM/TTL + 競合ロック + 実行タイムアウト  
Phase 3: ランタイム監視強化（レート制限、状態監査） + デバイスプロファイル拡充
